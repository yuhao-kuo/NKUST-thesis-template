
\usepackage{geometry}
\ifx\isatitlepage\undefined
    \geometry{
        paper=a4paper, % Default paper size, change to "letterpaper" for US Letter (you'll need to adjust margins after)
        top=1cm, % Top margin
        bottom=3cm, % bottom margin
        left=3cm,
        right=3cm,
        headheight=2cm, % Header height
        headsep=1cm, % Header separation
        includehead,
        includefoot
    }
\else
    \geometry{
        paper=a4paper, % Default paper size, change to "letterpaper" for US Letter (you'll need to adjust margins after)
        top=1cm, % Top margin
        bottom=3cm, % bottom margin
        left=3cm,
        right=3cm,
        headheight=2cm, % Header height
        headsep=1cm, % Header separation
        includehead,
        includefoot
    }
\fi
